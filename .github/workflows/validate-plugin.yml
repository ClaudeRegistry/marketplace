name: Validate Plugin Submission

on:
  pull_request:
    paths:
      - 'submissions/**'
      - 'plugins/**'
      - '.claude-plugin/**'
  push:
    branches:
      - main
    paths:
      - 'plugins/**'
      - '.claude-plugin/**'

jobs:
  validate:
    runs-on: ubuntu-latest
    name: Validate Plugin Structure

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install dependencies
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y -qq jq

      - name: Validate submissions
        run: |
          chmod +x .github/scripts/validate-plugins.sh
          .github/scripts/validate-plugins.sh submissions

      - name: Validate existing plugins (on push to main)
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        run: |
          chmod +x .github/scripts/validate-plugins.sh
          .github/scripts/validate-plugins.sh plugins

      - name: Comment on PR
        if: failure() && github.event_name == 'pull_request'
        uses: actions/github-script@v7
        with:
          github-token: ${{secrets.GITHUB_TOKEN}}
          script: |
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: '❌ Plugin validation failed. Please check the workflow logs for details and update your submission accordingly.\n\n**Validation checks include:**\n- Plugin structure (`.claude-plugin/plugin.json`)\n- Required metadata fields (name, version, description)\n- Documentation (`README.md`)\n- Command and skill file validation\n- Naming conventions\n- Marketplace registry (`.claude-plugin/marketplace.json`)\n- Registry consistency (all plugins listed)\n\nSee [CONTRIBUTING.md](../blob/main/CONTRIBUTING.md) for detailed guidelines on the required plugin structure.'
            })

      - name: Comment on PR (Success)
        if: success() && github.event_name == 'pull_request'
        uses: actions/github-script@v7
        with:
          github-token: ${{secrets.GITHUB_TOKEN}}
          script: |
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: '✅ Plugin validation passed! Your submission looks good.\n\n**Validation checks passed:**\n- ✅ Plugin structure validation\n- ✅ Metadata and manifest validation\n- ✅ Required files present\n- ✅ Naming conventions followed\n- ✅ Marketplace registry consistency\n\nA maintainer will review your plugin and provide feedback soon. Thank you for your contribution! 🚀'
            })
